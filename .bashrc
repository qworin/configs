alias ll='ls -la'
alias la='ls -a'
alias l='ls'
alias v='cd ~/proj'
alias js='node -p'
alias gti='git'
alias go='npm start'
alias goo='npm run dev'
alias g='grep'
alias gi='grep -i'
alias gr='grep -r'
alias gri='grep -ri'

if [ -f ~/git-flow-completion.bash ]; then
  . ~/git-flow-completion.bash
fi
